import 'package:flutter/material.dart';
class default_screen extends StatelessWidget {
  const default_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
